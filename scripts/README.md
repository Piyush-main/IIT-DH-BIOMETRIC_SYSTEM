# Scripts

One-off and maintenance scripts (e.g. bulk-importing a semester's student
roster from a registrar CSV, rotating admin emails) go here. Nothing yet —
add scripts as real operational needs come up rather than speculatively.
