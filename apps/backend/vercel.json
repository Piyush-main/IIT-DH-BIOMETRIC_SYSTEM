{
  "version": 2,
  "buildCommand": "cd ../.. && npm run build:shared && cd apps/backend && npx prisma generate && npm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
